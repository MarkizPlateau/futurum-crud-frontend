import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    main: "#4e5b6f",
    lightMode: {
      orange: "#dd6b20",
    },
    darkMode: {
      orange: "#fbd38d",
    },
  },
});

export default theme;
